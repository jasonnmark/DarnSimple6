if (Players.find().count() === 0) {
  Players.insert({
    name: 'John'
  });

  Players.insert({
    name: 'Jane'
  });

  Players.insert({
    name: 'June'
  });
}



/*
if (Notes.find().count() === 0) {
  Notes.insert({
    title: 'Introducing Telescope'
  });

  Notes.insert({
    title: 'Meteor'
  });

  Notes.insert({
    title: 'The Meteor Book'
  });
}
*/

