// Doesn't compile to CoffeeScript. Says "import" a reserved word when I try to do it manualy.

import { Meteor } from 'meteor/meteor';

Meteor.startup(() => {
  // code to run on server at startup
});
